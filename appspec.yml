version: 0.0
os: linux
files:
 - source: .
   destination: /home/ec2-user/quoterepo
hooks:
  AfterInstall:
   - location: scripts/installDep
     timeout: 300
     runas: root
  ApplicationStart:
   - location: scripts/start
     timeout: 300
     runas: root